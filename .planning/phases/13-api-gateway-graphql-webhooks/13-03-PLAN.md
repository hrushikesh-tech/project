---
phase: 13-api-gateway-graphql-webhooks
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - apps/api/src/accounts-payable/accounts-payable.controller.ts
  - apps/api/src/accounts-receivable/accounts-receivable.controller.ts
  - apps/api/src/finance/finance.controller.ts
  - apps/api/src/finance/finance-exception.filter.ts
  - apps/api/src/forecasting/forecasting.controller.ts
  - apps/api/src/hr/hr.controller.ts
  - apps/api/src/payroll/payroll.controller.ts
  - apps/api/src/project-management/project-management.controller.ts
  - apps/api/src/supply-chain/supply-chain.controller.ts
  - apps/api/test/integration/finance.api.test.mjs
  - apps/api/test/integration/finance-forecasting.api.test.mjs
  - apps/api/test/integration/hr.api.test.mjs
  - apps/api/test/integration/payroll.api.test.mjs
  - apps/api/test/integration/supply-chain.api.test.mjs
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
must_haves:
  truths:
    - "The full current REST surface is retrofitted in Phase 13 rather than leaving non-core ERP modules on legacy raw responses."
    - "Finance, AP/AR, HR, payroll, forecasting, supply chain, and project management all use centralized routing and the shared envelope."
    - "Legacy finance-specific exception formatting no longer creates an inconsistent error contract."
    - "Swagger docs cover the broader ERP modules after the core integration-facing modules establish the pattern."
  artifacts:
    - path: "apps/api/src/finance/finance.controller.ts"
      provides: "Documented finance REST surface on the shared platform contract"
    - path: "apps/api/src/accounts-payable/accounts-payable.controller.ts"
      provides: "AP contract aligned to centralized routing and envelopes"
    - path: "apps/api/src/hr/hr.controller.ts"
      provides: "HR contract aligned to shared docs and transport policy"
    - path: "apps/api/test/integration/finance.api.test.mjs"
      provides: "Representative guardrail for non-BI business-domain envelope behavior"
  key_links:
    - from: "apps/api/src/finance/finance-exception.filter.ts"
      to: "apps/api/src/common/api/api-exception.filter.ts"
      via: "shared error normalization"
      pattern: "error"
    - from: "apps/api/src/accounts-receivable/accounts-receivable.controller.ts"
      to: "apps/api/src/common/api/api-docs.ts"
      via: "shared response decorators"
      pattern: "Api"
    - from: "apps/api/test/integration/finance-forecasting.api.test.mjs"
      to: "apps/api/src/forecasting/forecasting.controller.ts"
      via: "envelope assertions for forecasting reads"
      pattern: "body"
---

<objective>
Complete the broad REST retrofit for the rest of the ERP modules: finance, AP/AR, forecasting, HR, payroll, supply chain, and project management. Move these controllers onto centralized versioning, apply the shared response/error envelope, and extend OpenAPI coverage so the entire current REST surface is standardized in this phase.

Purpose: Phase 13 explicitly chose a full retrofit instead of partial cleanup. This plan fulfills that decision so the API platform does not remain split between new and legacy transport contracts.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-CONTEXT.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-RESEARCH.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-01-PLAN.md
@apps/api/src/finance/finance.controller.ts
@apps/api/src/accounts-payable/accounts-payable.controller.ts
@apps/api/src/accounts-receivable/accounts-receivable.controller.ts
@apps/api/src/hr/hr.controller.ts
@apps/api/src/payroll/payroll.controller.ts
@apps/api/src/supply-chain/supply-chain.controller.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retrofit finance, forecasting, and AP/AR controllers onto the shared contract</name>
  <files>apps/api/src/finance/finance.controller.ts, apps/api/src/finance/finance-exception.filter.ts, apps/api/src/forecasting/forecasting.controller.ts, apps/api/src/accounts-payable/accounts-payable.controller.ts, apps/api/src/accounts-receivable/accounts-receivable.controller.ts, apps/api/test/integration/finance.api.test.mjs, apps/api/test/integration/finance-forecasting.api.test.mjs</files>
  <behavior>
    - Test 1: finance, forecasting, AP, and AR routes still resolve under `/api/v1/...` after controller prefix cleanup
    - Test 2: business-domain success payloads are wrapped by the shared envelope without changing calculation semantics
    - Test 3: finance-specific validation or domain failures return the standardized error contract
  </behavior>
  <action>Refactor the finance cluster controllers and filters to adopt the centralized prefix/versioning layer, envelope success/error contract, and Swagger conventions introduced in Wave 1. Preserve existing domain logic and tenant-safe behavior; only move the transport, docs, and exception seams onto the platform layer.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw -- finance.api.test.mjs finance-forecasting.api.test.mjs</automated>
  </verify>
  <done>The finance cluster now runs on the same routing, envelope, and docs contract as the core Phase 13 modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrofit HR and payroll controllers and lock their envelope behavior</name>
  <files>apps/api/src/hr/hr.controller.ts, apps/api/src/payroll/payroll.controller.ts, apps/api/test/integration/hr.api.test.mjs, apps/api/test/integration/payroll.api.test.mjs</files>
  <behavior>
    - Test 1: HR and payroll responses use `{ data, meta }`
    - Test 2: authorization and validation failures return `{ error, meta }`
    - Test 3: route URLs remain stable at `/api/v1/...`
  </behavior>
  <action>Apply the same centralized routing and transport standardization to HR and payroll, with route-level Swagger documentation and integration coverage that proves user-facing behavior did not regress while the response contract changed.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw -- hr.api.test.mjs payroll.api.test.mjs</automated>
  </verify>
  <done>HR and payroll are aligned to the shared Phase 13 API contract and protected by integration tests that assert the new envelope behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retrofit supply chain and project management and finish broad Swagger coverage</name>
  <files>apps/api/src/supply-chain/supply-chain.controller.ts, apps/api/src/project-management/project-management.controller.ts, apps/api/test/integration/supply-chain.api.test.mjs</files>
  <behavior>
    - Test 1: supply chain endpoints retain existing behavior while moving to the shared envelope
    - Test 2: project management endpoints are documented and version-platform owned
    - Test 3: no remaining controller in the current REST surface hardcodes `api/v1`
  </behavior>
  <action>Retrofit the remaining ERP controllers and complete Swagger tags/operation metadata so the current REST surface is consistently documented. Use this task to finish the phase decision that all current modules participate in the standardized API platform, not just the early adopter domains.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw -- supply-chain.api.test.mjs && rg "@Controller\\('api/v1|@Controller\\(\"api/v1" apps/api/src</automated>
  </verify>
  <done>All remaining business-domain controllers are migrated, documented, and free of controller-local `/api/v1` ownership.</done>
</task>

<task type="auto">
  <name>Task 4: Prove the API platform retrofit is broad, not partial</name>
  <files>apps/api/test/integration/finance.api.test.mjs, apps/api/test/integration/finance-forecasting.api.test.mjs, apps/api/test/integration/hr.api.test.mjs, apps/api/test/integration/payroll.api.test.mjs, apps/api/test/integration/supply-chain.api.test.mjs, .planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md</files>
  <action>Update representative business-domain integration suites and the validation note so the repo truthfully reflects a broad retrofit. The target state for this plan is that no current API module is left on an undocumented legacy response shape by accident.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run build && pnpm --filter @amdox/api run test:integration:raw -- finance.api.test.mjs finance-forecasting.api.test.mjs hr.api.test.mjs payroll.api.test.mjs supply-chain.api.test.mjs</automated>
  </verify>
  <done>The full REST surface is migrated strongly enough that later frontend and GraphQL work can treat the new API transport contract as universal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ERP users -> finance/HR/payroll/supply-chain APIs | Operational business data crosses from authenticated clients into tenant-scoped domain controllers |
| domain exceptions -> shared transport contract | Legacy module-specific errors must be normalized before clients consume them |
| OpenAPI docs -> internal integrations | Wider docs coverage must accurately reflect authorization and response semantics across many domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | finance exception path | mitigate | Replace ad hoc finance-specific error JSON with the shared error envelope so clients do not receive inconsistent failure contracts |
| T-13-08 | Information Disclosure | broad Swagger coverage | mitigate | Mark protected endpoints clearly and preserve existing guard behavior while documenting responses |
| T-13-09 | Repudiation | cross-module request tracing | mitigate | Use the shared requestId meta path everywhere so failures in operational modules are traceable consistently |
</threat_model>

<verification>
Run `pnpm --filter @amdox/api run build`.
Run `pnpm --filter @amdox/api run test:integration:raw -- finance.api.test.mjs finance-forecasting.api.test.mjs hr.api.test.mjs payroll.api.test.mjs supply-chain.api.test.mjs`.
Confirm no controller still hardcodes the API prefix with `rg "@Controller\\('api/v1|@Controller\\(\"api/v1" apps/api/src`.
</verification>

<success_criteria>
Phase 13's REST standardization is now genuinely repo-wide for the current API surface, not just demonstrated on a few modules. Every current ERP controller can be treated as centrally routed, envelope-based, and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/13-api-gateway-graphql-webhooks/13-03-SUMMARY.md`
</output>
