---
phase: 13-api-gateway-graphql-webhooks
plan: 05
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - apps/web/src/lib/api/client.ts
  - apps/web/src/lib/auth/session.ts
  - apps/api/test/integration/auth.api.test.mjs
  - apps/api/test/integration/bi.api.test.mjs
  - apps/api/test/integration/notifications.api.test.mjs
  - apps/api/test/integration/finance.api.test.mjs
  - apps/api/test/integration/finance-forecasting.api.test.mjs
  - apps/api/test/integration/hr.api.test.mjs
  - apps/api/test/integration/payroll.api.test.mjs
  - apps/api/test/integration/supply-chain.api.test.mjs
  - apps/api/test/integration/bi.graphql.test.mjs
  - .planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-04
  - API-05
must_haves:
  truths:
    - "Phase 13 is a hard cutover: frontend clients and integration tests move to the new API contract in the same phase."
    - "The web client understands the shared REST envelope and surfaces standardized error metadata instead of assuming raw JSON payloads."
    - "Validation proves REST standardization, Swagger/docs policy, and BI GraphQL policy together instead of testing them in isolation."
    - "The phase ends only after the API and the consuming client/test layers agree on the new contract."
  artifacts:
    - path: "apps/web/src/lib/api/client.ts"
      provides: "Envelope-aware API client unwrapping and standardized error handling"
    - path: "apps/web/src/lib/auth/session.ts"
      provides: "Auth/session client compatibility with the Phase 13 REST contract"
    - path: ".planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md"
      provides: "Truthful end-of-phase validation procedure and status"
    - path: "apps/api/test/integration/bi.graphql.test.mjs"
      provides: "GraphQL policy and contract verification guardrail"
  key_links:
    - from: "apps/web/src/lib/api/client.ts"
      to: "apps/api/src/common/api/api-success.interceptor.ts"
      via: "shared envelope unwrapping contract"
      pattern: "data|meta"
    - from: "apps/web/src/lib/auth/session.ts"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "auth flow reads against the new response shape"
      pattern: "login|refresh|me|logout"
    - from: ".planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md"
      to: "apps/api/test/integration/bi.graphql.test.mjs"
      via: "production policy verification steps"
      pattern: "introspection|trusted"
---

<objective>
Finish the Phase 13 cutover by updating consuming clients and verification assets to the new API platform contract, then run the integrated validation path across REST, docs policy, and BI GraphQL. This is the phase-close plan that proves the standardized platform is actually usable by the frontend and guarded by durable tests.

Purpose: a transport migration is not complete until the consumer layer and validation layer speak the same contract. This plan closes that loop.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-CONTEXT.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-RESEARCH.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-02-PLAN.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-03-PLAN.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-04-PLAN.md
@apps/web/src/lib/api/client.ts
@apps/web/src/lib/auth/session.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update the web client and auth session helpers for the enveloped REST contract</name>
  <files>apps/web/src/lib/api/client.ts, apps/web/src/lib/auth/session.ts</files>
  <behavior>
    - Test 1: auth login, refresh, me, and logout flows still work after the REST envelope cutover
    - Test 2: BI and notification reads can unwrap `{ data, meta }` without call-site churn leaking everywhere
    - Test 3: standardized API errors surface meaningful message and request metadata to the caller
  </behavior>
  <action>Update the shared web API client and auth/session helper layer so the frontend consumes the new Phase 13 success/error envelope centrally rather than forcing per-feature manual unwrapping. Preserve the existing auth and BI behavior while moving error parsing onto the new standardized contract.</action>
  <verify>
    <automated>pnpm --filter @amdox/web typecheck</automated>
  </verify>
  <done>The web client layer is envelope-aware, auth/session helpers still work, and consuming code can rely on one centralized unwrapping/error path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the integration suites to assert the real Phase 13 contract end to end</name>
  <files>apps/api/test/integration/auth.api.test.mjs, apps/api/test/integration/bi.api.test.mjs, apps/api/test/integration/notifications.api.test.mjs, apps/api/test/integration/finance.api.test.mjs, apps/api/test/integration/finance-forecasting.api.test.mjs, apps/api/test/integration/hr.api.test.mjs, apps/api/test/integration/payroll.api.test.mjs, apps/api/test/integration/supply-chain.api.test.mjs, apps/api/test/integration/bi.graphql.test.mjs</files>
  <behavior>
    - Test 1: representative REST suites assert `{ data, meta }` for success and `{ error, meta }` for failure
    - Test 2: requestId presence is asserted where Phase 13 requires it
    - Test 3: BI GraphQL tests cover auth, trusted operations, introspection, and complexity/depth policy
  </behavior>
  <action>Convert the representative integration suites from raw response assertions to the new transport contract and ensure GraphQL coverage verifies the locked production policy. Keep the tests focused on contract truth, not incidental implementation details, so they remain useful guardrails through future API versions.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw</automated>
  </verify>
  <done>The repo's integration layer now matches the actual Phase 13 API contract and will catch regressions in envelopes, docs policy assumptions, and BI GraphQL restrictions.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase with truthful validation and final cross-layer verification</name>
  <files>.planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md, apps/web/src/lib/api/client.ts, apps/web/src/lib/auth/session.ts, apps/api/test/integration/bi.graphql.test.mjs</files>
  <action>Update `13-VALIDATION.md` so it accurately reflects the final contract, commands, and pass/fail criteria. Then run the final phase-close verification path across the API build, integration suites, web typecheck, and any docs-policy checks introduced earlier in the phase. If any part of the hard cutover remains inconsistent, keep iterating until the validation note and the codebase tell the same story.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run build && pnpm --filter @amdox/api run test:integration:raw && pnpm --filter @amdox/web typecheck</automated>
  </verify>
  <done>Phase 13 ends with a truthful validation artifact and a verified consumer-compatible API platform, not just partially migrated backend code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend client -> standardized REST contract | Client parsing must safely handle the new envelope and preserve auth-sensitive behavior |
| test suites -> implementation truth | Validation loses value if tests still assert legacy shapes or ignore production GraphQL policy |
| validation note -> project state | The phase can be falsely marked ready if docs and actual verification drift apart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Repudiation | validation artifacts | mitigate | Update `13-VALIDATION.md` only after commands and coverage are real, so phase status can be trusted |
| T-13-14 | Denial of Service | frontend error handling | mitigate | Centralize envelope parsing and standardized error extraction instead of forcing brittle per-call behavior |
| T-13-15 | Tampering | test expectations | mitigate | Rewrite integration assertions to the Phase 13 contract so regressions in requestId/meta/error policy are caught automatically |
</threat_model>

<verification>
Run `pnpm --filter @amdox/api run build`.
Run `pnpm --filter @amdox/api run test:integration:raw`.
Run `pnpm --filter @amdox/web typecheck`.
Confirm the validation note mentions the final GraphQL policy and docs behavior with `rg "api-docs|introspection|trusted|requestId" .planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md`.
</verification>

<success_criteria>
The Phase 13 platform is consumed and verified end to end: backend routes are standardized, GraphQL is hardened, the frontend client understands the new contract, and the validation file truthfully describes a passing phase.
</success_criteria>

<output>
After completion, create `.planning/phases/13-api-gateway-graphql-webhooks/13-05-SUMMARY.md`
</output>
