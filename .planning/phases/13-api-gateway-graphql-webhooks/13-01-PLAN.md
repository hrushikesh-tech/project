---
phase: 13-api-gateway-graphql-webhooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - .env.example
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/common/api/request-id.constants.ts
  - apps/api/src/common/api/request-id.middleware.ts
  - apps/api/src/common/api/request-context.ts
  - apps/api/src/common/api/api-success.interceptor.ts
  - apps/api/src/common/api/api-exception.filter.ts
  - apps/api/src/common/api/api-pagination.ts
  - apps/api/src/common/api/api-docs.ts
  - apps/api/src/common/api/api-docs-basic-auth.middleware.ts
  - .planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-05
must_haves:
  truths:
    - "The API platform owns the `/api/v1/...` URL shape centrally instead of controllers hardcoding it."
    - "Every REST response can be wrapped with one shared success/error contract carrying requestId and timestamp."
    - "Swagger/OpenAPI and production docs gating are installed before route-by-route migration begins."
    - "Wave 2 work can assume the shared API platform and env contracts already exist."
  artifacts:
    - path: "apps/api/src/common/api/api-success.interceptor.ts"
      provides: "Shared REST success envelope"
    - path: "apps/api/src/common/api/api-exception.filter.ts"
      provides: "Shared REST error envelope"
    - path: "apps/api/src/common/api/api-docs.ts"
      provides: "Swagger/OpenAPI bootstrap and shared doc helpers"
    - path: "apps/api/src/common/api/request-id.middleware.ts"
      provides: "Request ID generation and propagation seam"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/common/api/api-docs.ts"
      via: "bootstrap configuration"
      pattern: "setupApiDocs"
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/common/api/api-success.interceptor.ts"
      via: "global interceptor registration"
      pattern: "APP_INTERCEPTOR"
    - from: "apps/api/src/common/api/api-exception.filter.ts"
      to: "apps/api/src/common/api/request-context.ts"
      via: "meta.requestId / meta.timestamp"
      pattern: "requestId"
---

<objective>
Establish the blocking API-platform foundation for Phase 13: install Swagger and GraphQL-adjacent dependencies, introduce centralized `/api/v1` routing via global prefix plus URI versioning, add request-ID propagation, add the shared REST success/error envelope infrastructure, and bootstrap `/api-docs` with production protection.

Purpose: every later Phase 13 task depends on one shared platform layer. This plan removes the current controller-local prefix drift, raw-response transport drift, and missing docs/bootstrap seams before route migration or GraphQL work begins.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-CONTEXT.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-RESEARCH.md
@.planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md
@apps/api/package.json
@apps/api/src/main.ts
@apps/api/src/app.module.ts
@apps/api/src/common/interceptors/audit.interceptor.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the blocking API platform dependencies and environment contract</name>
  <files>apps/api/package.json, pnpm-lock.yaml, .env.example</files>
  <action>Add the missing platform dependencies required for Swagger and schema-first GraphQL: `@nestjs/swagger`, `@nestjs/graphql`, `@nestjs/apollo`, `@apollo/server`, `graphql`, `dataloader`, and any small supporting package needed for docs protection if the implementation chooses one. Extend `.env.example` with the production policy controls Phase 13 needs, including docs enable/protect flags and GraphQL production enforcement settings. Do not add GraphQL client libraries or GraphOS-only infrastructure in this phase.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run build</automated>
  </verify>
  <done>`apps/api/package.json` contains the platform dependencies and `.env.example` documents the Phase 13 docs/GraphQL policy flags that later plans will rely on.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared request ID, envelope, pagination, and docs helper layer</name>
  <files>apps/api/src/common/api/request-id.constants.ts, apps/api/src/common/api/request-id.middleware.ts, apps/api/src/common/api/request-context.ts, apps/api/src/common/api/api-success.interceptor.ts, apps/api/src/common/api/api-exception.filter.ts, apps/api/src/common/api/api-pagination.ts, apps/api/src/common/api/api-docs.ts, apps/api/src/common/api/api-docs-basic-auth.middleware.ts</files>
  <action>Create one shared API-platform folder that owns request ID generation, meta construction, success envelopes, standardized error envelopes, optional pagination metadata helpers, and Swagger bootstrap/helpers. The envelope contract must match the locked Phase 13 shape: success `{ data, meta }`, error `{ error, meta }`, with requestId and timestamp always present. The docs helper must support `/api-docs` enabled by default in non-production and protected/optional in production. Do not push envelope logic down into domain services.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run build</automated>
  </verify>
  <done>The repo has one reusable API-platform layer for request IDs, envelopes, pagination metadata, and docs setup that later controller and GraphQL work can consume directly.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize `/api/v1` ownership in bootstrap and register the global platform providers</name>
  <files>apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/common/api/api-success.interceptor.ts, apps/api/src/common/api/api-exception.filter.ts, .planning/phases/13-api-gateway-graphql-webhooks/13-VALIDATION.md</files>
  <action>Update `main.ts` and `app.module.ts` so the application owns `app.setGlobalPrefix('api')`, URI versioning with default version `1`, request-ID middleware mounting, Swagger bootstrap, and global registration of the new success/error platform providers. Keep the existing guards and audit interceptor intact. Update `13-VALIDATION.md` only as needed so Wave 1 commands and file references remain truthful after this platform layer is in place.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run build && rg "setGlobalPrefix|enableVersioning|setupApiDocs|requestId" apps/api/src/main.ts apps/api/src/app.module.ts apps/api/src/common/api</automated>
  </verify>
  <done>`main.ts` owns centralized `/api/v1` behavior, the shared platform providers are registered globally, and Wave 2 can migrate controllers onto the new foundation without inventing its own bootstrap logic.</done>
</task>

<task type="auto">
  <name>Task 4: Run the blocking platform smoke checks before route or GraphQL work starts</name>
  <files>apps/api/package.json, apps/api/src/main.ts, apps/api/src/app.module.ts, .env.example</files>
  <action>After Tasks 1 through 3 land, run the blocking smoke path for this phase: install/sync dependencies if required, then run `pnpm --filter @amdox/api run build`. If the API package does not build cleanly with the new platform layer and dependencies, stop here and resolve that before Wave 2 begins. Do not start route migration or GraphQL implementation against a red platform baseline.</action>
  <verify>
    <automated>pnpm --filter @amdox/api run build</automated>
  </verify>
  <done>The Phase 13 platform baseline builds successfully and is safe for Wave 2 route migration and GraphQL work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Nest bootstrap | Untrusted HTTP requests enter platform-owned prefix/versioning/docs middleware here |
| controller return values -> transport contract | Domain payloads cross into the shared API envelope layer here |
| production operators -> `/api-docs` and GraphQL policy flags | Misconfigured env gating could expose internal API surfaces in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | `/api-docs` | mitigate | Default docs to non-prod enabled, prod opt-in, and protect prod docs with explicit credentials |
| T-13-02 | Tampering | shared response contract | mitigate | Keep one global envelope/error layer instead of letting modules drift to ad hoc JSON shapes |
| T-13-03 | Spoofing | request tracking | mitigate | Generate and propagate request IDs centrally so errors and docs/test assertions reference one stable request context |
</threat_model>

<verification>
Run `pnpm --filter @amdox/api run build`.
Confirm centralized bootstrap hooks exist with `rg "setGlobalPrefix|enableVersioning|setupApiDocs" apps/api/src/main.ts`.
Confirm shared API-platform files exist with `rg "requestId|meta|pagination|SwaggerModule" apps/api/src/common/api`.
</verification>

<success_criteria>
The API package now has the blocking Phase 13 platform layer: centralized `/api/v1` ownership, request IDs, shared success/error envelopes, Swagger bootstrap, and production docs gating. Wave 2 no longer needs to solve bootstrap or transport concerns ad hoc.
</success_criteria>

<output>
After completion, create `.planning/phases/13-api-gateway-graphql-webhooks/13-01-SUMMARY.md`
</output>
