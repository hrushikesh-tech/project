---
phase: 15-testing-strategy
plan: 04
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified:
  - package.json
  - tests/load
  - .planning/phases/15-testing-strategy/15-VALIDATION.md
autonomous: false
requirements:
  - TEST-01
  - TEST-03
  - TEST-05
must_haves:
  truths:
    - "k6 is wired into the repo as a real repeatable command."
    - "Load scenarios focus on API-heavy mixed traffic instead of pretending to cover every ERP flow equally."
    - "Validation records real concurrency, P95, and error-rate evidence."
    - "Phase 15 closes only after coverage, endpoint, journey, and load commands are all represented truthfully."
  artifacts:
    - path: "package.json"
      provides: "Root load-test entrypoint"
    - path: "tests/load"
      provides: "k6 scenarios and shared load-test helpers"
    - path: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      provides: "Final phase-close evidence"
  key_links:
    - from: "package.json"
      to: "tests/load"
      via: "repeatable load command wiring"
      pattern: "test:load"
    - from: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      to: "tests/load"
      via: "recorded P95/error-rate evidence"
      pattern: "k6|P95|error rate|2000"
---

<objective>
Close Phase 15 by adding a real k6 load-test layer for the API-heavy mixed workload, wiring a repeatable repo command for it, and updating the validation artifact with truthful final evidence across coverage, integration, E2E, smoke, and load verification.

Purpose: Phase 15 is not complete until the repo can prove performance behavior under the chosen workload mix and record those outcomes in a way later phases and CI can trust.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-testing-strategy/15-CONTEXT.md
@.planning/phases/15-testing-strategy/15-RESEARCH.md
@.planning/phases/15-testing-strategy/15-VALIDATION.md
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the API-heavy mixed k6 scenario suite and root load command</name>
  <files>package.json, tests/load</files>
  <read_first>
    - package.json
    - .planning/PROJECT.md
    - .planning/phases/15-testing-strategy/15-CONTEXT.md
    - .planning/phases/15-testing-strategy/15-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the repo exposes a repeatable `test:load` command
    - Test 2: the load suite models auth/session-sensitive routes, common CRUD, BI reads, and a bounded heavier slice
    - Test 3: the scenario reports the metrics needed to judge the Phase 15 SLA target
  </behavior>
  <action>Create a `tests/load` k6 suite and a root `test:load` script. Keep the scenario mix aligned with the locked decisions: API-heavy mixed traffic, not a full ERP simulator. Include at least auth/session-sensitive traffic, common CRUD/list traffic, BI reads, and a bounded heavy route class. Expose thresholds or assertions for concurrency, P95 latency, and error rate so the suite can support `TEST-05` truthfully.</action>
  <acceptance_criteria>
    - package.json contains `test:load`
    - `tests/load` exists
    - the k6 suite mentions auth/session-sensitive traffic
    - the k6 suite mentions BI reads or equivalent reporting traffic
    - the k6 suite contains thresholds for latency and error rate
  </acceptance_criteria>
  <verify>
    <automated>pnpm run test:load</automated>
  </verify>
  <done>The repo now has a real k6 layer aligned with the selected API-heavy workload, not just a placeholder performance requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Update the phase validation artifact with final evidence and caveats</name>
  <files>.planning/phases/15-testing-strategy/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-testing-strategy/15-VALIDATION.md
    - package.json
    - tests/load
    - apps/api/package.json
    - apps/web/playwright.config.ts
  </read_first>
  <action>Replace any remaining planning-time placeholders in `15-VALIDATION.md` with the actual final command set, execution notes, and environment caveats discovered during Phase 15. Record the final evidence for coverage, integration, smoke, E2E, and load commands truthfully. Do not mark the phase complete if any required command surface is still missing or if the load suite cannot produce the metrics needed for `TEST-05`.</action>
  <acceptance_criteria>
    - .planning/phases/15-testing-strategy/15-VALIDATION.md contains `updated:` only when final evidence is available
    - .planning/phases/15-testing-strategy/15-VALIDATION.md contains the final `test:load` command name
    - .planning/phases/15-testing-strategy/15-VALIDATION.md references P95 latency and error rate evidence
    - .planning/phases/15-testing-strategy/15-VALIDATION.md does not claim completion if a required command is absent
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw && pnpm --filter @amdox/web run test:e2e -- --workers=1 && pnpm run test:load</automated>
  </verify>
  <done>Phase 15 ends with a truthful validation record that can be consumed by later execution, review, and CI phases without hidden assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load scripts -> SLA truth | A decorative k6 suite can exist without proving the roadmap target |
| validation note -> future workflow trust | Later phases will rely on this artifact when deciding whether testing strategy is complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Repudiation | load evidence | mitigate | Add a real k6 command and thresholds for P95/error rate instead of advisory prose |
| T-15-10 | Denial of Service | performance confidence | mitigate | Keep scenarios aligned with the chosen API-heavy mixed workload and record actual caveats |
</threat_model>

<verification>
Run `pnpm run test:load`.
Run `pnpm --filter @amdox/api run test:integration:raw`.
Run `pnpm --filter @amdox/web run test:e2e -- --workers=1`.
Confirm final evidence references with `rg "test:load|k6|P95|error rate|2000" .planning/phases/15-testing-strategy/15-VALIDATION.md tests/load package.json`.
</verification>

<success_criteria>
Phase 15 closes with a real k6 workload, a repeatable load command, and a final validation artifact that truthfully records the end-state evidence for testing readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing-strategy/15-04-SUMMARY.md`
</output>
