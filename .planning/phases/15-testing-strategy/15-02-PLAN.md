---
phase: 15-testing-strategy
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - apps/api/test/integration/auth.api.test.mjs
  - apps/api/test/integration/apar.api.test.mjs
  - apps/api/test/integration/bi.api.test.mjs
  - apps/api/test/integration/finance.api.test.mjs
  - apps/api/test/integration/forecasting.jobs.test.mjs
  - apps/api/test/integration/hr.api.test.mjs
  - apps/api/test/integration/hr.leave-attendance.api.test.mjs
  - apps/api/test/integration/notifications.api.test.mjs
  - apps/api/test/integration/payroll.api.test.mjs
  - apps/api/test/integration/project-management.api.test.mjs
  - apps/api/test/integration/supply-chain.api.test.mjs
  - apps/api/test/smoke
  - apps/api/package.json
  - .planning/phases/15-testing-strategy/15-VALIDATION.md
autonomous: false
requirements:
  - TEST-02
  - TEST-04
must_haves:
  truths:
    - "Every API endpoint has at least one happy-path and one error-path assertion."
    - "The endpoint matrix stays on the fast harness-backed integration path."
    - "A smaller real-stack smoke suite proves live Postgres, Redis, and auth/runtime seams."
    - "The smoke layer supplements the matrix instead of replacing it."
  artifacts:
    - path: "apps/api/test/integration/*.test.mjs"
      provides: "Broad endpoint happy/error-path matrix for TEST-02"
    - path: "apps/api/test/smoke"
      provides: "Higher-fidelity runtime smoke layer for live infrastructure truth"
    - path: "apps/api/package.json"
      provides: "Repeatable smoke command entrypoint"
    - path: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      provides: "Truthful evidence for endpoint and smoke coverage"
  key_links:
    - from: "apps/api/test/integration/*.test.mjs"
      to: "apps/api/test/helpers/test-fixtures.mjs"
      via: "standardized tenant/user/seed setup"
      pattern: "createTestTenant|mockKeycloak"
    - from: "apps/api/test/smoke"
      to: "apps/api/package.json"
      via: "live-stack command wiring"
      pattern: "test:smoke"
    - from: "apps/api/test/integration/*.test.mjs"
      to: "apps/api/test/helpers/app-platform.mjs"
      via: "envelope/versioning assertions"
      pattern: "configureApiPlatform|unwrapBody"
---

<objective>
Expand Phase 15 backend verification to full API coverage by ensuring each endpoint has both happy-path and error-path integration tests, then add a bounded live-stack smoke suite that proves runtime behavior the harness cannot.

Purpose: `TEST-02` is the largest scope item in this phase. It must be satisfied systematically across the API surface while still leaving room for a small higher-fidelity suite that catches real environment drift.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-testing-strategy/15-CONTEXT.md
@.planning/phases/15-testing-strategy/15-RESEARCH.md
@.planning/phases/15-testing-strategy/15-VALIDATION.md
@apps/api/src
@apps/api/test/integration
@apps/api/test/helpers/app-platform.mjs
@apps/api/test/helpers/test-fixtures.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the harness-backed happy-path and error-path matrix for every API module</name>
  <files>apps/api/test/integration/auth.api.test.mjs, apps/api/test/integration/apar.api.test.mjs, apps/api/test/integration/bi.api.test.mjs, apps/api/test/integration/finance.api.test.mjs, apps/api/test/integration/forecasting.jobs.test.mjs, apps/api/test/integration/hr.api.test.mjs, apps/api/test/integration/hr.leave-attendance.api.test.mjs, apps/api/test/integration/notifications.api.test.mjs, apps/api/test/integration/payroll.api.test.mjs, apps/api/test/integration/project-management.api.test.mjs, apps/api/test/integration/supply-chain.api.test.mjs</files>
  <read_first>
    - apps/api/test/integration/auth.api.test.mjs
    - apps/api/test/integration/finance.api.test.mjs
    - apps/api/test/helpers/app-platform.mjs
    - apps/api/test/helpers/test-fixtures.mjs
    - .planning/phases/15-testing-strategy/15-CONTEXT.md
    - .planning/phases/15-testing-strategy/15-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: every module exposes at least one happy-path request per route family
    - Test 2: every module exposes at least one error-path assertion per route family
    - Test 3: integration tests keep asserting the Phase 13 envelope/platform contract instead of only raw bodies
  </behavior>
  <action>Audit the current route surface module by module and fill any missing happy-path or error-path gaps in the existing integration suites. Use the standardized helpers from Plan 01 for setup. Keep the suites harness-backed and focused on user-facing route behavior rather than implementation internals. Where endpoints share route families, one representative happy path and one representative error path are acceptable only if they actually prove the route family contract; do not leave uncovered route groups behind.</action>
  <acceptance_criteria>
    - every major integration suite under `apps/api/test/integration/` contains at least one explicit happy-path assertion
    - every major integration suite under `apps/api/test/integration/` contains at least one explicit error-path assertion
    - route families for auth, finance, AP/AR, HR, payroll, supply chain, BI, notifications, project management, and forecasting are all represented
    - suites continue using `configureApiPlatform` or equivalent shared platform setup
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw</automated>
  </verify>
  <done>The backend now has a deliberate endpoint matrix instead of a patchwork of route tests, satisfying the main breadth requirement of `TEST-02`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a bounded real-stack smoke suite for live Postgres, Redis, and auth/runtime seams</name>
  <files>apps/api/test/smoke, apps/api/package.json, .planning/phases/15-testing-strategy/15-VALIDATION.md</files>
  <read_first>
    - apps/api/package.json
    - .planning/codebase/TESTING.md
    - .planning/phases/14-security-hardening/14-VALIDATION.md
    - .planning/phases/15-testing-strategy/15-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the built API can boot against live infrastructure dependencies
    - Test 2: database and Redis-backed seams are exercised in a real environment
    - Test 3: auth/runtime behavior is proven end to end without trying to mirror the entire harness matrix
  </behavior>
  <action>Create a small live-stack smoke suite and script entrypoint, e.g. `test:smoke`, that targets the highest-value runtime truths the harness cannot prove. Keep it intentionally small: API boot, one database-backed happy path, one Redis/session-sensitive path, and one auth/runtime verification path. Reuse the current environment conventions already used in prior validation work where possible, and document any required env vars or service prerequisites truthfully in `15-VALIDATION.md`.</action>
  <acceptance_criteria>
    - `apps/api/package.json` contains `test:smoke`
    - `apps/api/test/smoke` exists
    - the smoke layer mentions live Postgres, Redis, or auth/runtime seams in code or docs
    - `.planning/phases/15-testing-strategy/15-VALIDATION.md` references the final smoke command and any runtime caveats truthfully
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:smoke</automated>
  </verify>
  <done>Phase 15 now has the higher-fidelity runtime proof the user wanted, without sacrificing the speed and breadth of the harness matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness-backed tests -> API contract truth | Missing negative paths can create false route confidence |
| live infrastructure -> smoke suite | The real-stack layer must prove runtime truths the harness cannot |
| route families -> requirements coverage | Incomplete route mapping would leave TEST-02 only partially satisfied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Repudiation | endpoint matrix | mitigate | Require explicit happy-path and error-path assertions across every API module |
| T-15-05 | Tampering | live-stack smoke | mitigate | Add a repeatable smoke command that exercises real Postgres, Redis, and auth/runtime seams |
| T-15-06 | Denial of Service | stale harness confidence | mitigate | Keep smoke focused on runtime truth instead of assuming harness breadth is enough |
</threat_model>

<verification>
Run `pnpm --filter @amdox/api run test:integration:raw`.
Run `pnpm --filter @amdox/api run test:smoke`.
Confirm route-family coverage with `rg "@Controller\\(|@Get\\(|@Post\\(|@Put\\(|@Patch\\(|@Delete\\(" apps/api/src`.
Confirm the smoke command exists with `rg "test:smoke" apps/api/package.json .planning/phases/15-testing-strategy/15-VALIDATION.md`.
</verification>

<success_criteria>
The API surface is now covered systematically through the harness matrix, and the repo also has a smaller live-stack smoke layer that proves runtime truths the harness alone cannot.
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing-strategy/15-02-SUMMARY.md`
</output>
