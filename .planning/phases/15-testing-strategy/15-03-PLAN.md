---
phase: 15-testing-strategy
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - apps/web/tests/e2e/helpers.ts
  - apps/web/tests/e2e/auth.spec.ts
  - apps/web/tests/e2e/auth-live.spec.ts
  - apps/web/tests/e2e/finance-apar-hr.spec.ts
  - apps/web/tests/e2e/payroll-supply-chain.spec.ts
  - apps/web/tests/e2e/bi-projects.spec.ts
  - apps/web/tests/e2e/offline-a11y.spec.ts
  - apps/web/tests/e2e/phase15-journeys.spec.ts
  - apps/web/playwright.config.ts
  - .planning/phases/15-testing-strategy/15-VALIDATION.md
autonomous: false
requirements:
  - TEST-03
must_haves:
  truths:
    - "The Playwright suite is organized around 8 business-critical journeys."
    - "Existing strong Phase 12 flows are reused rather than duplicated."
    - "Journey coverage stays cross-module and product-focused, not page-focused."
    - "The final journey command is stable enough to be part of phase validation."
  artifacts:
    - path: "apps/web/tests/e2e/*.spec.ts"
      provides: "Formal 8-journey Playwright product suite"
    - path: "apps/web/tests/e2e/helpers.ts"
      provides: "Shared login and journey support utilities"
    - path: "apps/web/playwright.config.ts"
      provides: "Stable execution contract for the formal journey suite"
    - path: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      provides: "Truthful E2E evidence and command references"
  key_links:
    - from: "apps/web/tests/e2e/helpers.ts"
      to: "apps/web/tests/e2e/*.spec.ts"
      via: "shared login and session-aware journey setup"
      pattern: "loginWithRetry"
    - from: "apps/web/playwright.config.ts"
      to: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      via: "formal journey command/runtime contract"
      pattern: "test:e2e"
---

<objective>
Formalize `TEST-03` by turning the current Playwright surface into 8 named, business-critical ERP journeys that reuse the strongest Phase 12 flows and close any remaining gaps without duplicating the suite unnecessarily.

Purpose: the repo already has good browser coverage, but it is still organized mostly by implementation slices. This plan elevates that work into a stable product-level journey suite that Phase 15 can defend and later CI can consume.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-testing-strategy/15-CONTEXT.md
@.planning/phases/15-testing-strategy/15-RESEARCH.md
@.planning/phases/12-frontend-next-js-15/12-VALIDATION.md
@apps/web/playwright.config.ts
@apps/web/tests/e2e
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map the existing Playwright surface into 8 named business journeys</name>
  <files>apps/web/tests/e2e/helpers.ts, apps/web/tests/e2e/auth.spec.ts, apps/web/tests/e2e/auth-live.spec.ts, apps/web/tests/e2e/finance-apar-hr.spec.ts, apps/web/tests/e2e/payroll-supply-chain.spec.ts, apps/web/tests/e2e/bi-projects.spec.ts, apps/web/tests/e2e/offline-a11y.spec.ts, apps/web/tests/e2e/phase15-journeys.spec.ts</files>
  <read_first>
    - apps/web/tests/e2e/helpers.ts
    - apps/web/tests/e2e/auth.spec.ts
    - apps/web/tests/e2e/finance-apar-hr.spec.ts
    - apps/web/tests/e2e/payroll-supply-chain.spec.ts
    - apps/web/tests/e2e/bi-projects.spec.ts
    - apps/web/tests/e2e/offline-a11y.spec.ts
    - .planning/phases/15-testing-strategy/15-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: there are 8 clearly named product journeys
    - Test 2: the strongest existing Phase 12 flows are reused instead of duplicated unnecessarily
    - Test 3: each journey proves a business workflow, not just page rendering
  </behavior>
  <action>Create or refactor the Playwright suite so the product-level journey catalog is explicit and totals 8 critical workflows. Reuse the current specs where they already prove the required behavior, and add only the missing journey glue or coverage gaps needed to reach a coherent 8-journey set. Keep the journeys cross-module where appropriate and make the naming obvious enough that Phase 15 validation can point to them directly.</action>
  <acceptance_criteria>
    - the final Playwright suite documents or encodes 8 named journeys
    - existing spec files are reused where they already prove the desired workflows
    - at least one explicit auth journey exists
    - at least one explicit finance/AP-AR or HR/payroll journey exists
    - at least one explicit supply-chain journey exists
    - at least one explicit BI/projects journey exists
    - at least one explicit offline/accessibility resilience journey exists
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/web run test:e2e -- --workers=1</automated>
  </verify>
  <done>The browser suite now expresses the 8 critical user journeys the roadmap asks for, grounded in the strongest existing Phase 12 flows.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize the formal journey execution contract and validation references</name>
  <files>apps/web/playwright.config.ts, .planning/phases/15-testing-strategy/15-VALIDATION.md</files>
  <read_first>
    - apps/web/playwright.config.ts
    - .planning/phases/12-frontend-next-js-15/12-VALIDATION.md
    - .planning/phases/15-testing-strategy/15-VALIDATION.md
  </read_first>
  <action>Keep the Playwright runtime contract explicit and stable for Phase 15. If the formal journey suite needs serial execution, external-server support, or clearer environment variables, encode that directly in config/docs rather than leaving it as tribal knowledge. Update `15-VALIDATION.md` so the final E2E command and journey references match the suite created in this plan.</action>
  <acceptance_criteria>
    - `apps/web/playwright.config.ts` still defines a clear `baseURL` contract
    - `.planning/phases/15-testing-strategy/15-VALIDATION.md` references the final journey command truthfully
    - the validation note reflects any required serial execution or env prerequisites
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/web run test:e2e -- --workers=1</automated>
  </verify>
  <done>The Playwright journey suite is now stable enough to be treated as a release-grade product test layer instead of an evolving ad hoc browser suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser journeys -> product confidence | Weak or duplicated browser tests can appear broad without proving real workflows |
| Playwright config -> execution truth | Hidden environment assumptions can make the journey suite unreliable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Repudiation | E2E journey catalog | mitigate | Make the 8 journeys explicit and business-oriented rather than implicit in file names |
| T-15-08 | Denial of Service | flaky browser execution | mitigate | Keep runtime assumptions explicit and use the stable serial command in validation if needed |
</threat_model>

<verification>
Run `pnpm --filter @amdox/web run test:e2e -- --workers=1`.
Confirm the journey surface with `rg "test\\(|describe\\(" apps/web/tests/e2e`.
Confirm validation references with `rg "test:e2e|journey" .planning/phases/15-testing-strategy/15-VALIDATION.md apps/web/tests/e2e`.
</verification>

<success_criteria>
Phase 15 has a defendable 8-journey Playwright suite that reuses the strongest existing ERP browser flows and runs under a stable, explicit execution contract.
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing-strategy/15-03-SUMMARY.md`
</output>
