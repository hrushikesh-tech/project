---
phase: 15-testing-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web/package.json
  - apps/api/test/helpers/test-fixtures.mjs
  - apps/api/test/helpers/finance-test-store.mjs
  - apps/api/test/helpers/supply-chain-test-store.mjs
  - apps/api/test/helpers/prisma-client.mjs
  - .planning/phases/15-testing-strategy/15-VALIDATION.md
autonomous: true
requirements:
  - TEST-01
  - TEST-04
must_haves:
  truths:
    - "Coverage reporting exists as a real command, not just a requirement written in docs."
    - "The hard >=80% floor targets core service logic instead of thin controllers or DTO wrappers."
    - "Shared helpers expose stable entrypoints for tenant/user setup, cleanup, auth mocking, and finance/inventory seeding."
    - "Phase 15 validation references only commands and files that actually exist."
  artifacts:
    - path: "apps/api/package.json"
      provides: "Coverage and helper/smoke script contract for backend testing"
    - path: "apps/web/package.json"
      provides: "Frontend coverage and formal journey command contract if needed"
    - path: "apps/api/test/helpers/test-fixtures.mjs"
      provides: "Standardized reusable helper surface for TEST-04"
    - path: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      provides: "Truthful validation contract for the phase"
  key_links:
    - from: "apps/api/test/helpers/test-fixtures.mjs"
      to: "apps/api/test/helpers/finance-test-store.mjs"
      via: "shared seedFinanceData helper"
      pattern: "seedFinanceData"
    - from: "apps/api/test/helpers/test-fixtures.mjs"
      to: "apps/api/test/helpers/supply-chain-test-store.mjs"
      via: "shared seedInventoryData helper"
      pattern: "seedInventoryData"
    - from: "apps/api/package.json"
      to: ".planning/phases/15-testing-strategy/15-VALIDATION.md"
      via: "real command names used in validation"
      pattern: "coverage|test:smoke|test:load"
---

<objective>
Establish the Phase 15 testing foundation by adding real coverage/reporting commands, defining the service-targeted threshold contract, standardizing the shared fixture/helper API, and updating the validation artifact to point at actual executable commands.

Purpose: the rest of Phase 15 depends on truthful measurement and reusable fixtures. Without this plan, later endpoint, Playwright, and k6 work would sit on unstable or duplicated testing infrastructure.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-testing-strategy/15-CONTEXT.md
@.planning/phases/15-testing-strategy/15-RESEARCH.md
@.planning/phases/15-testing-strategy/15-VALIDATION.md
@apps/api/package.json
@apps/web/package.json
@apps/api/test/helpers
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add concrete coverage/reporting commands and service-focused threshold wiring</name>
  <files>apps/api/package.json, apps/web/package.json, .planning/phases/15-testing-strategy/15-VALIDATION.md</files>
  <read_first>
    - apps/api/package.json
    - apps/web/package.json
    - .planning/phases/15-testing-strategy/15-CONTEXT.md
    - .planning/phases/15-testing-strategy/15-RESEARCH.md
    - .planning/codebase/TESTING.md
  </read_first>
  <behavior>
    - Test 1: the repo exposes a real coverage command instead of only raw unit commands
    - Test 2: the coverage contract makes core service logic the hard floor and leaves thin transport layers advisory
    - Test 3: validation docs reference the final command names truthfully
  </behavior>
  <action>Add executable coverage/reporting script wiring for the backend test surface, and add any frontend coverage command only if it supports the final validation story without diluting the user-selected service-first rule. The command must build or otherwise avoid stale-dist reporting problems. Encode the service-focused threshold policy in the chosen coverage config or runner wiring so core service classes are the hard target for `TEST-01`. Update `15-VALIDATION.md` so its command references match the real coverage entrypoints introduced here.</action>
  <acceptance_criteria>
    - apps/api/package.json contains a script with `coverage` in the name
    - apps/api/package.json no longer relies on raw unit tests alone to satisfy `TEST-01`
    - the coverage wiring references service-oriented files or thresholds rather than only aggregate percentages
    - .planning/phases/15-testing-strategy/15-VALIDATION.md contains the exact final coverage command name
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run build</automated>
  </verify>
  <done>The repo can now measure Phase 15 coverage truthfully and enforce the chosen service-focused threshold contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Standardize the shared fixture API for tenants, users, auth mocking, and domain seeding</name>
  <files>apps/api/test/helpers/test-fixtures.mjs, apps/api/test/helpers/finance-test-store.mjs, apps/api/test/helpers/supply-chain-test-store.mjs, apps/api/test/helpers/prisma-client.mjs</files>
  <read_first>
    - apps/api/test/helpers/finance-test-store.mjs
    - apps/api/test/helpers/supply-chain-test-store.mjs
    - apps/api/test/helpers/prisma-client.mjs
    - .planning/phases/15-testing-strategy/15-CONTEXT.md
    - .planning/phases/15-testing-strategy/15-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: tests can create and clean up a tenant through one shared helper surface
    - Test 2: tests can create a user and mock auth/session context without re-implementing Keycloak seams
    - Test 3: tests can seed finance and inventory data through named shared helpers rather than direct store-specific setup in every file
  </behavior>
  <action>Create a shared helper module that exports explicit Phase 15 utility entrypoints named `createTestTenant`, `createTestUser`, `cleanupTestTenant`, `mockKeycloak`, `seedFinanceData`, and `seedInventoryData`. Reuse the existing domain stores and helper modules internally rather than replacing them. Keep the helper API small and composable so later integration, smoke, and E2E tests can adopt it without a framework rewrite.</action>
  <acceptance_criteria>
    - apps/api/test/helpers/test-fixtures.mjs exists
    - apps/api/test/helpers/test-fixtures.mjs exports `createTestTenant`
    - apps/api/test/helpers/test-fixtures.mjs exports `createTestUser`
    - apps/api/test/helpers/test-fixtures.mjs exports `cleanupTestTenant`
    - apps/api/test/helpers/test-fixtures.mjs exports `mockKeycloak`
    - apps/api/test/helpers/test-fixtures.mjs exports `seedFinanceData`
    - apps/api/test/helpers/test-fixtures.mjs exports `seedInventoryData`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:unit:raw</automated>
  </verify>
  <done>Phase 15 now has the shared helper contract required by `TEST-04`, built on top of the repo’s existing harness assets instead of parallel ad hoc fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Make the validation contract truthful for the rest of the phase</name>
  <files>.planning/phases/15-testing-strategy/15-VALIDATION.md, apps/api/package.json, apps/web/package.json</files>
  <read_first>
    - .planning/phases/15-testing-strategy/15-VALIDATION.md
    - apps/api/package.json
    - apps/web/package.json
  </read_first>
  <action>Replace any draft placeholders in `15-VALIDATION.md` that depend on Phase 15 foundation work with the actual commands and file paths created in this plan. Keep wave-0 items only where infrastructure is still intentionally deferred to later plans. Do not claim smoke or k6 commands exist until they do.</action>
  <acceptance_criteria>
    - .planning/phases/15-testing-strategy/15-VALIDATION.md references real scripts from `apps/api/package.json` or `apps/web/package.json`
    - .planning/phases/15-testing-strategy/15-VALIDATION.md does not claim a finished load command unless one exists
    - .planning/phases/15-testing-strategy/15-VALIDATION.md keeps `status: draft`
  </acceptance_criteria>
  <verify>
    <automated>rg "coverage|test:smoke|test:load" .planning/phases/15-testing-strategy/15-VALIDATION.md apps/api/package.json apps/web/package.json</automated>
  </verify>
  <done>The phase validation artifact is now aligned with the real testing foundation instead of speculative future commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner config -> coverage truth | Incorrect coverage wiring could create false confidence about the service floor |
| shared fixtures -> all future tests | A broken helper API would spread brittle setup patterns across the phase |
| validation docs -> execution workflow | Stale commands in the validation note would mislead later execution and verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Repudiation | coverage reporting | mitigate | Add real commands and thresholds rather than relying on advisory statements |
| T-15-02 | Tampering | shared helper contract | mitigate | Centralize tenant/user/auth/seed helpers behind one reusable module |
| T-15-03 | Repudiation | validation artifact | mitigate | Update `15-VALIDATION.md` only to reflect actual scripts and remaining wave-0 gaps |
</threat_model>

<verification>
Run `pnpm --filter @amdox/api run build`.
Run `pnpm --filter @amdox/api run test:unit:raw`.
Confirm the helper exports with `rg "createTestTenant|createTestUser|cleanupTestTenant|mockKeycloak|seedFinanceData|seedInventoryData" apps/api/test/helpers/test-fixtures.mjs`.
Confirm the validation note references real scripts with `rg "coverage|test:smoke|test:load" .planning/phases/15-testing-strategy/15-VALIDATION.md apps/api/package.json apps/web/package.json`.
</verification>

<success_criteria>
Phase 15 now has measurable coverage commands, a service-focused threshold contract, a standardized fixture API for TEST-04, and a truthful validation baseline that later plans can build on without re-litigating the test infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing-strategy/15-01-SUMMARY.md`
</output>
