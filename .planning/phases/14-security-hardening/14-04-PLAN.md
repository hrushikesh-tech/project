---
phase: 14-security-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - package.json
  - scripts/security/run-trufflehog.ps1
  - apps/api/test/integration/auth.api.test.mjs
  - apps/api/test/integration/apar.api.test.mjs
  - apps/api/test/integration/bi.api.test.mjs
  - apps/api/test/integration/finance.api.test.mjs
  - apps/api/test/integration/hr.api.test.mjs
  - apps/api/test/integration/hr.leave-attendance.api.test.mjs
  - apps/api/test/integration/notifications.api.test.mjs
  - apps/api/test/integration/payroll.api.test.mjs
  - apps/api/test/integration/project-management.api.test.mjs
  - apps/api/test/integration/supply-chain.api.test.mjs
  - apps/api/test/integration/security-auth.api.test.mjs
  - apps/api/test/integration/security-throttling.api.test.mjs
  - .planning/phases/14-security-hardening/14-VALIDATION.md
autonomous: false
requirements:
  - SEC-02
  - SEC-06
  - SEC-08
  - SEC-09
must_haves:
  truths:
    - "Every protected business module has an explicit cross-tenant denial proof, not just HR/payroll."
    - "Session revocation, refresh replay rejection, and 429 throttling are verified by integration tests."
    - "The repo has a repeatable local secrets-scan entry point."
    - "The validation note tells the truth about what passed, what was run, and any remaining environment caveats."
  artifacts:
    - path: "apps/api/test/integration/security-auth.api.test.mjs"
      provides: "Auth/session security regression coverage"
    - path: "apps/api/test/integration/security-throttling.api.test.mjs"
      provides: "Route-aware 429 regression coverage"
    - path: "scripts/security/run-trufflehog.ps1"
      provides: "Local secrets-scan entry point"
    - path: ".planning/phases/14-security-hardening/14-VALIDATION.md"
      provides: "Truthful final evidence for Phase 14"
  key_links:
    - from: "scripts/security/run-trufflehog.ps1"
      to: "package.json"
      via: "repeatable local invocation"
      pattern: "security:secrets"
    - from: "apps/api/test/integration/security-auth.api.test.mjs"
      to: "apps/api/src/auth/auth.service.ts"
      via: "rotation/revocation/session-cap verification"
      pattern: "refresh|logout|UserSession"
    - from: ".planning/phases/14-security-hardening/14-VALIDATION.md"
      to: "apps/api/test/integration/security-throttling.api.test.mjs"
      via: "phase-close evidence"
      pattern: "429|cross-tenant|trufflehog"
---

<objective>
Close Phase 14 with broad verification and repo-hygiene enforcement: add dedicated security regression suites, prove cross-tenant denial across all protected modules, add a repeatable secrets-scan entry point, and update the validation artifact with the actual final evidence.

Purpose: security hardening is not complete until the repo can prove the controls work and can re-run those proofs later without relying on tribal memory.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-security-hardening/14-CONTEXT.md
@.planning/phases/14-security-hardening/14-RESEARCH.md
@.planning/phases/14-security-hardening/14-VALIDATION.md
@apps/api/test/integration
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated auth/session and throttling security regression suites</name>
  <files>apps/api/test/integration/security-auth.api.test.mjs, apps/api/test/integration/security-throttling.api.test.mjs, apps/api/test/integration/auth.api.test.mjs</files>
  <read_first>
    - apps/api/test/integration/auth.api.test.mjs
    - apps/api/src/auth/auth.service.ts
    - apps/api/src/common/security/rate-limit.guard.ts
    - .planning/phases/14-security-hardening/14-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: replayed or revoked refresh tokens fail
    - Test 2: the 6th session is blocked
    - Test 3: auth/OCR/payroll/default thresholds return `429` at the expected boundary
  </behavior>
  <action>Create `apps/api/test/integration/security-auth.api.test.mjs` for the Phase 14 auth-specific regressions: refresh replay rejection, revoked-session rejection, logout revocation, and max-5-session enforcement. Create `apps/api/test/integration/security-throttling.api.test.mjs` for the exact policy map introduced in Plan 02, covering default, auth, OCR, and payroll thresholds. Keep `apps/api/test/integration/auth.api.test.mjs` for the functional auth contract, but move the new denial-path security assertions into the dedicated security suites so they remain focused and durable.</action>
  <acceptance_criteria>
    - apps/api/test/integration/security-auth.api.test.mjs exists
    - apps/api/test/integration/security-auth.api.test.mjs contains a refresh replay or revoked-session assertion
    - apps/api/test/integration/security-auth.api.test.mjs contains a max-session assertion
    - apps/api/test/integration/security-throttling.api.test.mjs exists
    - apps/api/test/integration/security-throttling.api.test.mjs contains `429`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw</automated>
  </verify>
  <done>The repo has dedicated security regression suites for the two hardest runtime guarantees in Phase 14: session correctness and route-aware throttling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove cross-tenant denial across every protected business module</name>
  <files>apps/api/test/integration/apar.api.test.mjs, apps/api/test/integration/bi.api.test.mjs, apps/api/test/integration/finance.api.test.mjs, apps/api/test/integration/hr.api.test.mjs, apps/api/test/integration/hr.leave-attendance.api.test.mjs, apps/api/test/integration/notifications.api.test.mjs, apps/api/test/integration/payroll.api.test.mjs, apps/api/test/integration/project-management.api.test.mjs, apps/api/test/integration/supply-chain.api.test.mjs</files>
  <read_first>
    - apps/api/test/integration/hr.api.test.mjs
    - apps/api/test/integration/payroll.api.test.mjs
    - apps/api/src/common/guards/tenant.guard.ts
    - apps/api/src/prisma/prisma.service.ts
    - .planning/phases/14-security-hardening/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: every protected module has at least one explicit cross-tenant `403` assertion
    - Test 2: super-admin cross-tenant access succeeds only when an explicit tenant header is provided
    - Test 3: the test suite fails if any module quietly accepts a mismatched tenant header or cross-tenant resource ID
  </behavior>
  <action>Use the existing HR/payroll tenant-denial tests as the template and extend the pattern into AP/AR, BI, finance, notifications, project management, and supply chain integration suites. For each module, add at least one protected endpoint test where the authenticated tenant is `tenant-1` and the request attempts to read or mutate a `tenant-2` resource or header override, expecting `403`. Add one positive super-admin test path only where an explicit `x-tenant-id` is intentionally provided, so the suite proves the explicit-switch model instead of blanket-blocking all admin traffic.</action>
  <acceptance_criteria>
    - apps/api/test/integration/apar.api.test.mjs contains `403`
    - apps/api/test/integration/bi.api.test.mjs contains `403`
    - apps/api/test/integration/finance.api.test.mjs contains `403`
    - apps/api/test/integration/notifications.api.test.mjs contains `403`
    - apps/api/test/integration/project-management.api.test.mjs contains `403`
    - apps/api/test/integration/supply-chain.api.test.mjs contains `403`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw</automated>
  </verify>
  <done>`SEC-08` is proven across the whole ERP API surface rather than inferred from two modules.</done>
</task>

<task type="auto">
  <name>Task 3: Add the local secrets-scan entry point and remove any remaining committed runtime defaults in scope</name>
  <files>package.json, scripts/security/run-trufflehog.ps1</files>
  <read_first>
    - package.json
    - .planning/phases/14-security-hardening/14-RESEARCH.md
    - infra/keycloak/amdox-realm.json
    - apps/api/src/auth/auth.service.ts
    - apps/web/src/auth.ts
    - scripts/phase12-auth-proxy.mjs
  </read_first>
  <action>Create `scripts/security/run-trufflehog.ps1` as the local entry point for secrets scanning and add a root `package.json` script named `security:secrets` that invokes it. The PowerShell script must run `trufflehog` against the current repository, exit non-zero on findings, and fail loudly with an installation hint if `trufflehog` is unavailable. Before wiring the command, remove any Phase 14-scoped committed runtime fallback secrets that remain in source so the scan can realistically pass: backend auth default client-secret fallback, frontend auth fallback secret, and helper-script dev-secret fallbacks. Keep harmless test fixture values isolated to tests rather than runtime code paths.</action>
  <acceptance_criteria>
    - package.json contains `"security:secrets"`
    - scripts/security/run-trufflehog.ps1 exists
    - scripts/security/run-trufflehog.ps1 contains `trufflehog`
    - apps/api/src/auth/auth.service.ts does not contain `amdox-api-dev-secret`
    - apps/web/src/auth.ts does not contain `amdox-phase12-dev-secret`
    - scripts/phase12-auth-proxy.mjs does not contain `amdox-api-dev-secret`
  </acceptance_criteria>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/security/run-trufflehog.ps1</automated>
  </verify>
  <done>The repo has a repeatable local secrets-scan command and no longer relies on committed runtime fallback secrets in the main auth path.</done>
</task>

<task type="auto">
  <name>Task 4: Update the validation artifact with the real final evidence and environment notes</name>
  <files>.planning/phases/14-security-hardening/14-VALIDATION.md</files>
  <read_first>
    - .planning/phases/14-security-hardening/14-VALIDATION.md
    - apps/api/test/integration/security-auth.api.test.mjs
    - apps/api/test/integration/security-throttling.api.test.mjs
    - package.json
    - scripts/security/run-trufflehog.ps1
  </read_first>
  <action>Replace the draft placeholders in `14-VALIDATION.md` with the actual final commands, dates, pass/fail outcomes, and any remaining environment caveats discovered during execution. Mark `wave_0_complete` and `nyquist_compliant` truthfully based on the evidence created in this phase. Do not mark the phase complete if the secrets-scan command is missing, the security suites are red, or any required cross-tenant module coverage is still absent.</action>
  <acceptance_criteria>
    - .planning/phases/14-security-hardening/14-VALIDATION.md contains `updated:`
    - .planning/phases/14-security-hardening/14-VALIDATION.md contains `security-auth.api.test.mjs`
    - .planning/phases/14-security-hardening/14-VALIDATION.md contains `security-throttling.api.test.mjs`
    - .planning/phases/14-security-hardening/14-VALIDATION.md contains `security:secrets`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @amdox/api run test:integration:raw && pnpm --filter @amdox/web run test:unit && pnpm --filter @amdox/web build</automated>
  </verify>
  <done>The phase ends with a truthful validation contract that future workflows can trust, instead of a stale planning-time placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| security regression tests -> implementation truth | The phase can only be trusted if the tests assert the real security guarantees |
| repository contents -> secrets scan | Committed defaults and helper scripts can leak secrets even if the runtime code is otherwise hardened |
| validation note -> project state | A false green validation file would make later workflows trust incomplete security work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Repudiation | validation artifacts | mitigate | Update `14-VALIDATION.md` only after real execution results exist |
| T-14-09 | Information Disclosure | committed secrets | mitigate | Add a repeatable local secrets-scan entry point and remove runtime fallback secrets from source |
| T-14-10 | Elevation of Privilege | tenant-isolation regressions | mitigate | Add explicit `403` assertions across every protected module, not only a subset |
</threat_model>

<verification>
Run `pnpm --filter @amdox/api run test:integration:raw`.
Run `pnpm --filter @amdox/web run test:unit`.
Run `pnpm --filter @amdox/web build`.
Run `powershell -ExecutionPolicy Bypass -File scripts/security/run-trufflehog.ps1`.
Confirm validation evidence with `rg "security-auth|security-throttling|security:secrets|403|429" .planning/phases/14-security-hardening/14-VALIDATION.md apps/api/test/integration`.
</verification>

<success_criteria>
Phase 14 closes with broad, durable proof: security regression suites exist, every protected module has explicit cross-tenant denial coverage, a local secrets-scan command exists, and the validation artifact truthfully records the final evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/14-security-hardening/14-04-SUMMARY.md`
</output>
