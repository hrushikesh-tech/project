---
phase: 12-frontend-next-js-15
plan: 06
type: execute
wave: 6
depends_on:
  - 12-01
  - 12-02
  - 12-03
  - 12-04
  - 12-05
files_modified:
  - apps/web/app/manifest.ts
  - apps/web/public/icon-192.png
  - apps/web/public/icon-512.png
  - apps/web/public/offline.html
  - apps/web/src/sw.ts
  - apps/web/src/lib/offline/db.ts
  - apps/web/src/lib/offline/policy.ts
  - apps/web/src/lib/offline/sync.ts
  - apps/web/src/components/offline/offline-banner.tsx
  - apps/web/src/components/offline/sync-queue-panel.tsx
  - apps/web/src/lib/accessibility/a11y-audit.ts
  - apps/web/tests/e2e/offline-a11y.spec.ts
  - .planning/phases/12-frontend-next-js-15/12-VALIDATION.md
autonomous: false
requirements:
  - UI-10
  - UI-11
  - UI-12
must_haves:
  truths:
    - "The app has a manifest, service worker, and offline entry path."
    - "Only low-risk draft and preference mutations are queued offline, with visible sync state."
    - "High-risk finance, payroll, inventory, and dependency-sensitive actions remain online-only."
    - "Critical flows meet WCAG 2.1 AA expectations and the required 375px, 768px, and 1440px responsive behavior."
    - "Phase 12 closes with real verification evidence, not placeholders."
  artifacts:
    - path: "apps/web/app/manifest.ts"
      provides: "PWA manifest"
    - path: "apps/web/src/lib/offline/policy.ts"
      provides: "Selective queue allowlist and online-only boundaries"
    - path: "apps/web/src/components/offline/offline-banner.tsx"
      provides: "Visible offline and sync-state messaging"
    - path: ".planning/phases/12-frontend-next-js-15/12-VALIDATION.md"
      provides: "Final verification evidence"
  key_links:
    - from: "apps/web/src/lib/offline/policy.ts"
      to: "apps/web/src/lib/offline/sync.ts"
      via: "allowlisted queue execution"
      pattern: "allow|deny"
    - from: "apps/web/src/components/offline/offline-banner.tsx"
      to: "apps/web/src/components/offline/sync-queue-panel.tsx"
      via: "user-visible queue status"
      pattern: "queued|syncing|failed"
    - from: ".planning/phases/12-frontend-next-js-15/12-VALIDATION.md"
      to: "apps/web/tests/e2e/offline-a11y.spec.ts"
      via: "final evidence path"
      pattern: "offline-a11y"
---

<objective>
Close Phase 12 by adding the bounded PWA/offline layer, hardening accessibility and responsive behavior across the built modules, and recording the final verification evidence for the phase.

Purpose: This plan turns the built frontend into a trustworthy ERP client by enforcing the locked offline boundaries, the accessibility and responsiveness bar, and the final validation trail needed before Phase 12 execution is considered complete.
Output: Manifest and service worker wiring, IndexedDB-backed selective offline queue, visible offline/sync UI, accessibility/responsive hardening, final validation evidence, and a human verification gate for the highest-risk UX behaviors.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-frontend-next-js-15/12-CONTEXT.md
@.planning/phases/12-frontend-next-js-15/12-RESEARCH.md
@.planning/phases/12-frontend-next-js-15/12-VALIDATION.md
@apps/web/app/(dashboard)
@apps/web/src/components
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the selective offline PWA layer and visible sync-state UX</name>
  <files>apps/web/app/manifest.ts, apps/web/public/icon-192.png, apps/web/public/icon-512.png, apps/web/public/offline.html, apps/web/src/sw.ts, apps/web/src/lib/offline/db.ts, apps/web/src/lib/offline/policy.ts, apps/web/src/lib/offline/sync.ts, apps/web/src/components/offline/offline-banner.tsx, apps/web/src/components/offline/sync-queue-panel.tsx</files>
  <behavior>
    - Test 1: manifest and service worker are registered and core shell routes remain available for offline recovery
    - Test 2: allowlisted low-risk draft and preference actions are queued in IndexedDB and replay with last-write-wins semantics
    - Test 3: high-risk actions are blocked from offline persistence and surface clear online-only messaging
  </behavior>
  <action>Implement UI-12 per D-16, D-17, D-18, and D-19 using the research-recommended PWA stack. Build an explicit allowlist in `policy.ts` for low-risk drafts and preferences only, keep finance posting, payroll operations, inventory-affecting actions, and dependency-sensitive project scheduling online-only, and expose queue state in the shell rather than hiding it. Do not broaden offline support beyond the locked scope.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:e2e -- offline-a11y.spec.ts</automated>
  </verify>
  <done>The web app has a real PWA shell, an IndexedDB-backed selective offline queue, visible sync state, and enforced online-only boundaries for high-risk workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the final accessibility, responsiveness, and evidence hardening pass</name>
  <files>apps/web/src/lib/accessibility/a11y-audit.ts, apps/web/tests/e2e/offline-a11y.spec.ts, .planning/phases/12-frontend-next-js-15/12-VALIDATION.md</files>
  <action>Perform the closeout pass for UI-10 and UI-11 across the shell and module surfaces already built. Verify keyboard reachability, labels, focus visibility, contrast, responsive behavior at 375px, 768px, and 1440px, and accessibility on the specialized BI, Gantt, heatmap, payroll, and offline queue interactions. Update `12-VALIDATION.md` with the actual final commands and results, including Lighthouse-style evidence where available.</action>
  <verify>
    <automated>pnpm --filter @amdox/web lint &amp;&amp; pnpm --filter @amdox/web typecheck &amp;&amp; pnpm --filter @amdox/web run test:unit &amp;&amp; pnpm --filter @amdox/web run test:e2e &amp;&amp; pnpm --filter @amdox/web build</automated>
  </verify>
  <done>Accessibility and responsive hardening are complete across the full frontend surface, and `12-VALIDATION.md` contains real final evidence instead of draft placeholders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the highest-risk interactive UX behaviors</name>
  <files>.planning/phases/12-frontend-next-js-15/12-VALIDATION.md</files>
  <action>Pause after the automated closeout work completes and have the user verify the highest-risk interactive UX behaviors that automated tests cannot fully judge on their own: role-home feel, BI drag experience, bounded Gantt scheduling behavior, offline queue clarity, and responsive ergonomics.</action>
  <what-built>Complete Phase 12 frontend including auth shell, module screens, BI builder, Gantt scheduling, inventory heatmap, and selective offline queue.</what-built>
  <how-to-verify>
    1. Sign in and confirm you land on the correct role home first inside the unified shell.
    2. Visit Finance, Payroll, Supply Chain, BI, Projects, and Notifications to confirm the shell stays consistent while each module preserves dense operational behavior.
    3. In BI, drag and resize widgets and confirm the layout persists without exposing any free-form metric builder behavior.
    4. In Projects, load the seeded large-task project and confirm dependency arrows remain visible while drag-to-reschedule stays bounded.
    5. Simulate offline mode, queue one allowed draft or preference action, and confirm online-only messaging blocks a high-risk action.
    6. Spot-check mobile, tablet, and desktop widths for layout stability and keyboard navigation.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @amdox/web run test:e2e -- offline-a11y.spec.ts</automated>
  </verify>
  <done>The user has either approved the final UX behaviors or returned a concrete issue list to address before sign-off.</done>
  <resume-signal>Type "approved" or describe the issue list to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline queue -> backend mutations | Stored client actions may replay against backend state later |
| accessibility helpers -> all screens | Final a11y defaults affect every module surface |
| final validation docs -> phase sign-off | Inaccurate evidence would misstate readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering | offline queue and sync layer | mitigate | Enforce an explicit allowlist, last-write-wins only for low-risk queued actions, and deny high-risk workflows from offline persistence |
| T-12-17 | Repudiation | offline and accessibility evidence | mitigate | Record final automated results in `12-VALIDATION.md` and require a final human verification gate for the riskiest UX interactions |
| T-12-18 | Information Disclosure | responsive and accessibility regressions | mitigate | Run full-shell accessibility and responsive checks so hidden or collapsed content does not expose or mask protected information incorrectly |
</threat_model>

<verification>
Run `pnpm --filter @amdox/web lint`.
Run `pnpm --filter @amdox/web typecheck`.
Run `pnpm --filter @amdox/web run test:unit`.
Run `pnpm --filter @amdox/web run test:e2e`.
Run `pnpm --filter @amdox/web build`.
Confirm offline allowlist seams with `rg "allow|deny|queued|syncing|failed" apps/web/src/lib/offline apps/web/src/components/offline`.
</verification>

<success_criteria>
Phase 12 closes with a bounded, honest offline story, WCAG-aware and responsive module behavior, and a final validation trail that demonstrates the frontend is ready for downstream verification and security hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-next-js-15/12-06-SUMMARY.md`
</output>
