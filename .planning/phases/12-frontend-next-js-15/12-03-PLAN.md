---
phase: 12-frontend-next-js-15
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - apps/web/src/lib/api/client.ts
  - apps/web/src/lib/query/keys.ts
  - apps/web/src/components/data-table/app-data-table.tsx
  - apps/web/src/components/forms/app-form.tsx
  - apps/web/src/components/module-home/module-home-hero.tsx
  - apps/web/app/(dashboard)/finance/page.tsx
  - apps/web/app/(dashboard)/finance/journal-entry/page.tsx
  - apps/web/app/(dashboard)/ap-ar/page.tsx
  - apps/web/app/(dashboard)/ap-ar/invoices/page.tsx
  - apps/web/app/(dashboard)/hr/page.tsx
  - apps/web/app/(dashboard)/hr/employees/page.tsx
  - apps/web/app/(dashboard)/notifications/preferences/page.tsx
  - apps/web/tests/unit/operational-ui.test.tsx
  - apps/web/tests/e2e/finance-apar-hr.spec.ts
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-10
  - UI-11
must_haves:
  truths:
    - "Operational tables share sorting, filtering, search, pagination, selection, export, and state handling."
    - "Operational forms share RHF + Zod validation, submit disablement, toast feedback, and unsaved-change protection."
    - "Finance users can build journal entries with real-time balance and FX preview."
    - "AP/AR and HR have real dense module surfaces inside the shared shell."
  artifacts:
    - path: "apps/web/src/components/data-table/app-data-table.tsx"
      provides: "Reusable UI-03 table behavior"
    - path: "apps/web/src/components/forms/app-form.tsx"
      provides: "Reusable UI-04 form behavior"
    - path: "apps/web/app/(dashboard)/finance/journal-entry/page.tsx"
      provides: "Journal entry UX for UI-05"
    - path: "apps/web/app/(dashboard)/ap-ar/invoices/page.tsx"
      provides: "AP/AR operational surface"
  key_links:
    - from: "apps/web/src/lib/api/client.ts"
      to: "apps/web/src/lib/query/keys.ts"
      via: "typed data-fetching layer"
      pattern: "queryKey"
    - from: "apps/web/app/(dashboard)/finance/journal-entry/page.tsx"
      to: "packages/types/src/finance.ts"
      via: "shared finance contracts"
      pattern: "finance"
    - from: "apps/web/src/components/forms/app-form.tsx"
      to: "apps/web/app/(dashboard)/notifications/preferences/page.tsx"
      via: "shared form infrastructure"
      pattern: "useForm|zod"
---

<objective>
Deliver the shared operational UX layer and the first major module surfaces: Finance, AP/AR, HR, and notification preferences/admin forms.

Purpose: This plan turns the shell into a real ERP work surface by standardizing the dense table/form patterns and applying them to business modules that depend on precision, validation, and strong state handling.
Output: Shared table and form components, typed API/query adapters, finance journal entry UX, AP/AR operational pages, HR screens, and notification preference forms.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-frontend-next-js-15/12-CONTEXT.md
@.planning/phases/12-frontend-next-js-15/12-RESEARCH.md
@packages/types/src/finance.ts
@packages/types/src/hr.ts
@packages/types/src/notifications.ts
@apps/api/src/finance/finance-journal-entries.controller.ts
@apps/api/src/finance/finance-reports.controller.ts
@apps/api/src/hr/hr.controller.ts
@apps/api/src/notifications/notifications.controller.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared operational data and form layer</name>
  <files>apps/web/src/lib/api/client.ts, apps/web/src/lib/query/keys.ts, apps/web/src/components/data-table/app-data-table.tsx, apps/web/src/components/forms/app-form.tsx, apps/web/src/components/module-home/module-home-hero.tsx</files>
  <behavior>
    - Test 1: table wrappers expose sorting, filters, debounced global search, server pagination, row selection, export, and retryable error states
    - Test 2: form wrappers expose RHF + Zod parsing, inline errors, disabled-submit states, toasts, and unsaved-change warning hooks
  </behavior>
  <action>Create the reusable platform for UI-03 and UI-04. Keep server state in TanStack Query and UI state in local/Zustand seams per D-03; do not move fetched datasets into ad hoc global stores. The shared patterns here must support dense enterprise workflows first per D-07, D-08, and D-09, while still giving module home surfaces the richer visual highlights expected by the hybrid shell.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:unit -- operational-ui</automated>
  </verify>
  <done>The app has one reusable operational layer for tables, forms, module-home hero states, and typed query access, and later module plans can consume these patterns instead of rebuilding them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver Finance and AP/AR screens with real journal-entry behavior</name>
  <files>apps/web/app/(dashboard)/finance/page.tsx, apps/web/app/(dashboard)/finance/journal-entry/page.tsx, apps/web/app/(dashboard)/ap-ar/page.tsx, apps/web/app/(dashboard)/ap-ar/invoices/page.tsx</files>
  <behavior>
    - Test 1: journal entry lines can be added and removed while balance totals update in real time
    - Test 2: account fuzzy search resolves account choices from shared finance data
    - Test 3: multi-currency FX preview updates before submission and surfaces backend constraint failures clearly
  </behavior>
  <action>Implement UI-05 in full, not as a placeholder. The journal-entry screen must support dynamic lines, real-time debit and credit balancing, account search, and FX preview using the Phase 3 finance seams. Add AP/AR invoice and aging-facing operational screens in the same shared table/form language so finance and AP/AR feel like one ERP surface rather than disconnected pages.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:e2e -- finance-apar-hr.spec.ts</automated>
  </verify>
  <done>Finance users can perform the actual journal-entry workflow required by UI-05, and AP/AR surfaces exist on top of the shared operational components.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver HR and notification preference/admin surfaces on the shared operational layer</name>
  <files>apps/web/app/(dashboard)/hr/page.tsx, apps/web/app/(dashboard)/hr/employees/page.tsx, apps/web/app/(dashboard)/notifications/preferences/page.tsx, apps/web/tests/unit/operational-ui.test.tsx, apps/web/tests/e2e/finance-apar-hr.spec.ts</files>
  <behavior>
    - Test 1: HR tables reuse the shared sorting, filter, and pagination behavior
    - Test 2: notification preference forms reuse RHF + Zod validation and unsaved-change warnings
    - Test 3: module surfaces remain accessible at keyboard and screen-reader level
  </behavior>
  <action>Implement HR employee and leave-facing screens plus notification preferences/admin forms using the shared operational scaffolding from Task 1. This is where the cross-module consistency decision in D-06 becomes visible: HR and notifications should feel like modules inside the same ERP shell, not custom one-offs.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:unit &amp;&amp; pnpm --filter @amdox/web build</automated>
  </verify>
  <done>HR and notification preference/admin flows are live in the shell, share the same operational patterns as Finance/AP/AR, and preserve the accessibility and responsive standards required by UI-10 and UI-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> operational forms | Untrusted business data crosses into client validation and submission paths |
| table controls -> API/query layer | Sorting/filtering/export controls drive backend-backed reads |
| finance UI -> backend constraints | The journal-entry UI must not misrepresent accounting validity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | shared form layer | mitigate | Enforce RHF + Zod validation and consistent submit/error handling on all new forms |
| T-12-08 | Repudiation | finance journal entry UX | mitigate | Surface real-time balancing, FX preview, and backend error states so the UI does not hide invalid accounting inputs |
| T-12-09 | Information Disclosure | shared data-table layer | mitigate | Keep all operational reads behind typed query adapters and protected-shell session context rather than route params alone |
</threat_model>

<verification>
Run `pnpm --filter @amdox/web run test:unit`.
Run `pnpm --filter @amdox/web run test:e2e`.
Run `pnpm --filter @amdox/web build`.
Confirm UI-03 and UI-04 patterns with `rg "useForm|zod|pagination|globalFilter|export" apps/web/src apps/web/app`.
</verification>

<success_criteria>
The ERP now has a consistent dense operational UX layer and real Finance, AP/AR, HR, and notification preference/admin surfaces. UI-03, UI-04, and UI-05 are no longer abstract requirements; they are implemented patterns and screens inside the shared shell.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-next-js-15/12-03-SUMMARY.md`
</output>
