---
phase: 12-frontend-next-js-15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vitest.config.ts
  - apps/web/playwright.config.ts
  - apps/web/app/layout.tsx
  - apps/web/app/globals.css
  - apps/web/app/(auth)/layout.tsx
  - apps/web/app/(dashboard)/layout.tsx
  - apps/web/src/providers/app-providers.tsx
  - packages/ui/package.json
  - packages/ui/src/index.ts
  - packages/ui/src/primitives/button.tsx
  - packages/ui/src/forms/form-field.tsx
  - packages/ui/src/data-display/data-table-shell.tsx
  - .planning/phases/12-frontend-next-js-15/12-VALIDATION.md
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-10
  - UI-11
must_haves:
  truths:
    - "The web app runs on Next.js 15 with a real App Router foundation."
    - "Public auth routes and protected dashboard routes exist as separate route groups."
    - "Shared table, form, and accessibility primitives exist before feature screens start."
    - "Frontend unit and E2E commands exist and are wired into phase validation."
  artifacts:
    - path: "apps/web/app/(auth)/layout.tsx"
      provides: "Auth route-group entry point"
    - path: "apps/web/app/(dashboard)/layout.tsx"
      provides: "Protected shell entry point"
    - path: "packages/ui/src/data-display/data-table-shell.tsx"
      provides: "Reusable table foundation for UI-03"
    - path: "apps/web/vitest.config.ts"
      provides: "Frontend unit/component test configuration"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/src/providers/app-providers.tsx"
      via: "root provider composition"
      pattern: "AppProviders"
    - from: "apps/web/app/(dashboard)/layout.tsx"
      to: "packages/ui/src/index.ts"
      via: "shared shell primitives"
      pattern: "@amdox/ui"
    - from: ".planning/phases/12-frontend-next-js-15/12-VALIDATION.md"
      to: "apps/web/package.json"
      via: "real scripts and commands"
      pattern: "test:unit|test:e2e|lint|typecheck|build"
---

<objective>
Establish the executable frontend baseline for Phase 12 by correcting the platform to Next.js 15, creating the App Router route-group skeleton, promoting `packages/ui` into a real shared UI package, and installing the frontend validation stack that later plans rely on.

Purpose: Phase 12 is still starting from a stub web app and an empty UI package; this plan removes that structural drift so later plans can implement module UX without inventing foundations ad hoc.
Output: A Next.js 15 baseline, `(auth)` and `(dashboard)` route groups, shared UI/table/form primitives, and working frontend test/build scripts.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-frontend-next-js-15/12-CONTEXT.md
@.planning/phases/12-frontend-next-js-15/12-RESEARCH.md
@.planning/phases/12-frontend-next-js-15/12-VALIDATION.md
@apps/web/package.json
@apps/web/app/layout.tsx
@apps/web/app/page.tsx
@packages/ui/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the web package to Next.js 15 and install the frontend runtime plus test stack</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/vitest.config.ts, apps/web/playwright.config.ts</files>
  <action>Correct the web baseline to Next.js 15 per D-01 and D-02, keeping the stack direction in D-03. Install the core runtime and validation dependencies from research: Auth.js/next-auth v5, TanStack Query, Zustand, React Hook Form, Zod, TanStack Table, and the frontend test stack. Tighten the TypeScript baseline so the new UI work does not inherit the current loose scaffold defaults. Do not keep `next@16.x`, and do not defer the missing frontend test infrastructure.</action>
  <verify>
    <automated>pnpm --filter @amdox/web lint &amp;&amp; pnpm --filter @amdox/web typecheck</automated>
  </verify>
  <done>`apps/web/package.json` targets Next.js 15, frontend scripts exist for `lint`, `typecheck`, `test:unit`, `test:e2e`, and the web package is ready for real implementation work instead of scaffold drift.</done>
</task>

<task type="auto">
  <name>Task 2: Create the App Router shell skeleton and shared UI foundations</name>
  <files>apps/web/app/layout.tsx, apps/web/app/globals.css, apps/web/app/(auth)/layout.tsx, apps/web/app/(dashboard)/layout.tsx, apps/web/src/providers/app-providers.tsx, packages/ui/package.json, packages/ui/src/index.ts, packages/ui/src/primitives/button.tsx, packages/ui/src/forms/form-field.tsx, packages/ui/src/data-display/data-table-shell.tsx</files>
  <action>Build the initial route and component foundation for UI-01, UI-03, UI-04, UI-10, and UI-11. Create `(auth)` and `(dashboard)` route groups per D-04 and D-05, wire root providers in one place, and turn `packages/ui` into a real shared package with accessible primitives, form wrappers, table shells, loading states, and focus-visible treatments. Keep these foundations generic and reusable; do not leak module-specific business logic into `packages/ui`.</action>
  <verify>
    <automated>pnpm --filter @amdox/web build</automated>
  </verify>
  <done>The route groups exist, the root layout mounts provider composition instead of a placeholder tree, and `packages/ui` exports reusable primitives that enforce the baseline accessibility and responsive behavior expected by later plans.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the draft validation baseline with real frontend verification paths</name>
  <files>.planning/phases/12-frontend-next-js-15/12-VALIDATION.md, apps/web/package.json, apps/web/vitest.config.ts, apps/web/playwright.config.ts</files>
  <action>Update `12-VALIDATION.md` so every referenced script, config file, and Wave 0 dependency is real. Replace placeholder verification language with the actual frontend commands created in this plan, including unit/component coverage and Playwright entry points. This task closes the research-identified Wave 0 gap instead of leaving `MISSING` verification references behind.</action>
  <verify>
    <automated>pnpm --filter @amdox/web lint &amp;&amp; pnpm --filter @amdox/web typecheck &amp;&amp; pnpm --filter @amdox/web build</automated>
  </verify>
  <done>`12-VALIDATION.md` points at real web test/build files and commands, and the phase has a concrete automated feedback loop before any feature-heavy UI work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next.js app | Untrusted route access and user input enter the frontend here |
| shared UI -> feature screens | Accessibility and interaction defaults propagate across the product |
| validation docs -> executor commands | Incorrect validation references would produce false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `apps/web/package.json` | mitigate | Pin the baseline back to Next.js 15 per D-01 and D-02 and validate with build/typecheck commands |
| T-12-02 | Elevation of Privilege | route-group foundation | mitigate | Separate `(auth)` and `(dashboard)` roots now so later auth work has a protected shell seam |
| T-12-03 | Information Disclosure | shared UI defaults | mitigate | Put focus, label, and state scaffolds into shared primitives before screens are built |
</threat_model>

<verification>
Run `pnpm --filter @amdox/web lint`.
Run `pnpm --filter @amdox/web typecheck`.
Run `pnpm --filter @amdox/web build`.
Confirm route groups exist with `rg "(auth)|(dashboard)" apps/web/app`.
Confirm shared UI exports exist with `rg "export" packages/ui/src`.
</verification>

<success_criteria>
The frontend is now a real Next.js 15 application skeleton rather than a placeholder. Shared table/form/accessibility primitives and frontend test commands exist, and the rest of Phase 12 can build on one coherent baseline instead of recreating infrastructure plan by plan.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-next-js-15/12-01-SUMMARY.md`
</output>
