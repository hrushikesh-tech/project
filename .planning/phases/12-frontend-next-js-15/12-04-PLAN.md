---
phase: 12-frontend-next-js-15
plan: 04
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - apps/web/app/(dashboard)/payroll/page.tsx
  - apps/web/app/(dashboard)/payroll/runs/page.tsx
  - apps/web/app/(dashboard)/supply-chain/page.tsx
  - apps/web/app/(dashboard)/supply-chain/inventory/page.tsx
  - apps/web/app/(dashboard)/notifications/page.tsx
  - apps/web/src/components/payroll/payroll-run-dashboard.tsx
  - apps/web/src/components/payroll/payslip-actions.tsx
  - apps/web/src/components/supply-chain/inventory-heatmap.tsx
  - apps/web/src/components/notifications/notification-center.tsx
  - apps/web/src/lib/payroll/progress-client.ts
  - apps/web/tests/e2e/payroll-supply-chain.spec.ts
  - apps/web/tests/unit/inventory-heatmap.test.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-06
  - UI-07
  - UI-10
  - UI-11
must_haves:
  truths:
    - "Payroll users can monitor run progress, preview payslips, bulk email, and download ZIP artifacts."
    - "Supply Chain users can read inventory state in a warehouse x product heatmap."
    - "Notifications have a real in-shell center, not just preference forms."
    - "These specialized screens still follow the shared table/form/accessibility patterns."
  artifacts:
    - path: "apps/web/src/components/payroll/payroll-run-dashboard.tsx"
      provides: "Payroll run UX for UI-06"
    - path: "apps/web/src/components/supply-chain/inventory-heatmap.tsx"
      provides: "Inventory heatmap for UI-07"
    - path: "apps/web/src/components/notifications/notification-center.tsx"
      provides: "Notification inbox surface"
    - path: "apps/web/src/lib/payroll/progress-client.ts"
      provides: "Payroll progress transport client"
  key_links:
    - from: "apps/web/src/lib/payroll/progress-client.ts"
      to: "apps/web/src/components/payroll/payroll-run-dashboard.tsx"
      via: "live progress updates"
      pattern: "progress"
    - from: "apps/web/src/components/supply-chain/inventory-heatmap.tsx"
      to: "packages/types/src/supply-chain.ts"
      via: "warehouse and stock contracts"
      pattern: "warehouse|inventory"
    - from: "apps/web/app/(dashboard)/notifications/page.tsx"
      to: "apps/web/src/components/notifications/notification-center.tsx"
      via: "shell route composition"
      pattern: "NotificationCenter"
---

<objective>
Deliver Payroll, Supply Chain, and Notifications module surfaces, including the payroll operations dashboard and the bounded inventory heatmap experience.

Purpose: These modules add the specialized operational screens that Phase 12 requires beyond generic tables/forms, while preserving the shared shell and enterprise-core interaction model established earlier in the phase.
Output: Payroll run dashboard and artifact actions, supply-chain inventory heatmap and supporting screens, and a first-class notification center.
</objective>

<execution_context>
@C:/Users/91892/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/91892/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-frontend-next-js-15/12-CONTEXT.md
@.planning/phases/12-frontend-next-js-15/12-RESEARCH.md
@packages/types/src/payroll.ts
@packages/types/src/supply-chain.ts
@packages/types/src/notifications.ts
@apps/api/src/payroll/payroll.controller.ts
@apps/api/src/supply-chain/supply-chain.controller.ts
@apps/api/src/notifications/notifications.controller.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver the payroll operations dashboard with live progress and artifact actions</name>
  <files>apps/web/app/(dashboard)/payroll/page.tsx, apps/web/app/(dashboard)/payroll/runs/page.tsx, apps/web/src/components/payroll/payroll-run-dashboard.tsx, apps/web/src/components/payroll/payslip-actions.tsx, apps/web/src/lib/payroll/progress-client.ts</files>
  <behavior>
    - Test 1: payroll run progress updates stream into the dashboard without forcing a full page reload
    - Test 2: users can preview payslips and access bulk email plus ZIP download actions
    - Test 3: error and retry states preserve the shared operational UX patterns
  </behavior>
  <action>Implement UI-06 in full. Use the payroll progress transport expected by the requirement and backend seam, keep progress handling isolated in a dedicated client, and expose payslip preview, bulk email, and ZIP download actions in the same surface. Do not simplify this to static status polling if the progress stream contract exists; the plan must satisfy the live progress requirement exactly.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:e2e -- payroll-supply-chain.spec.ts</automated>
  </verify>
  <done>The payroll dashboard shows real progress and artifact actions, and payroll users can manage runs from the frontend without losing the shared shell consistency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver Supply Chain operational screens and the inventory heatmap</name>
  <files>apps/web/app/(dashboard)/supply-chain/page.tsx, apps/web/app/(dashboard)/supply-chain/inventory/page.tsx, apps/web/src/components/supply-chain/inventory-heatmap.tsx, apps/web/tests/unit/inventory-heatmap.test.tsx</files>
  <behavior>
    - Test 1: the inventory heatmap renders a warehouse x product grid with clear color-coded stock levels
    - Test 2: keyboard and screen-reader users can understand the heatmap state through accessible labels and summaries
    - Test 3: the supply-chain screens remain usable at mobile, tablet, and desktop breakpoints
  </behavior>
  <action>Implement UI-07 per D-07 through D-09: keep the surrounding supply-chain workflows dense and operational, but let the inventory heatmap carry the visual highlight treatment needed for fast stock-state scanning. Preserve bounded semantics from Phase 7; this is an ERP inventory view, not a free-form analytics canvas.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:unit -- inventory-heatmap</automated>
  </verify>
  <done>The supply-chain module has real operational screens and an accessible, color-coded inventory heatmap that matches UI-07.</done>
</task>

<task type="auto">
  <name>Task 3: Add the notification center and lock the specialized module flows with tests</name>
  <files>apps/web/app/(dashboard)/notifications/page.tsx, apps/web/src/components/notifications/notification-center.tsx, apps/web/tests/e2e/payroll-supply-chain.spec.ts</files>
  <action>Build the in-shell notification center so the shell has a real inbox surface in addition to the preference forms from Plan 03. Then extend automated coverage so payroll, supply-chain, and notification-center interactions are all verified together against the shared shell and module-navigation behavior.</action>
  <verify>
    <automated>pnpm --filter @amdox/web run test:unit &amp;&amp; pnpm --filter @amdox/web run test:e2e &amp;&amp; pnpm --filter @amdox/web build</automated>
  </verify>
  <done>Payroll, Supply Chain, and Notifications all have working module surfaces inside the ERP shell, and the most specialized interactions in this slice are covered by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| progress transport -> payroll UI | Live status crosses from backend event transport into user-visible operational controls |
| inventory data -> heatmap rendering | Stock-state data becomes visual status signals that users act on |
| notification data -> in-shell inbox | Event content flows into a protected user-facing center |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Repudiation | payroll run dashboard | mitigate | Surface real progress, artifact states, and explicit errors instead of static or inferred status |
| T-12-11 | Information Disclosure | inventory heatmap and notification center | mitigate | Keep both surfaces behind the protected shell and use the shared session-aware data layer |
| T-12-12 | Denial of Service | specialized module rendering | mitigate | Reuse shared operational components and test realistic data paths so the most interactive module screens do not collapse under normal load |
</threat_model>

<verification>
Run `pnpm --filter @amdox/web run test:unit`.
Run `pnpm --filter @amdox/web run test:e2e`.
Run `pnpm --filter @amdox/web build`.
Confirm payroll and heatmap seams with `rg "progress|heatmap|notification" apps/web/src apps/web/app`.
</verification>

<success_criteria>
Payroll, Supply Chain, and Notifications are now real frontend modules inside the ERP shell. UI-06 and UI-07 are implemented with the required specialized interactions, and those surfaces still conform to the shared enterprise-core design and validation patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-next-js-15/12-04-SUMMARY.md`
</output>
